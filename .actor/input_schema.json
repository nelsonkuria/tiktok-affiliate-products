{
  "title": "Fetch UFC data",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "endpoint": {
      "title": "Endpoint",
      "type": "string",
      "description": "The endpoint to fetch data from.",
      "editor": "textfield",
      "prefill": "/products"
    },
    "params": {
      "title": "Params",
      "type": "object",
      "description": "Search params",
      "editor": "json"
    }
  },
  "required": ["resource", "params"]
}
